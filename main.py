import max_profit as mp
import prime_number as pn

if __name__ != '__main__':
    pn.is_prime_number(188)
    pn.is_prime_number(138)
    pn.is_prime_number(248)
    pn.is_prime_number(978)
    pn.is_prime_number(378)
